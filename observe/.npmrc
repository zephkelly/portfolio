# Hoist transitive deps to the top-level node_modules (npm-style flat layout).
# Nuxt and its modules resolve packages like @nuxt/kit from the project root;
# pnpm's default strict layout hides them, which breaks `nuxt build`.
# Mirrors the portfolio's own .npmrc.
shamefully-hoist=true
strict-peer-dependencies=false
